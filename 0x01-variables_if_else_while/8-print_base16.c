#include <stdio.h>
/**
* main - prints all numbers from base 16 in lowercase
*
* Return: Always 0 (success)
*/
int main(void)
{
	char c;

	for (c = 'a'; c <= 'p'; c++)
		putchar(c);

	putchar('\n');
	return (0);
}
