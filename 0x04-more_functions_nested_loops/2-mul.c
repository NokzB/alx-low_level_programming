#include "main.h"

/**
* mul - a functuib that mutiplies two numbers
* @a: integer with a number
* @b: integer with a number
* Return: 1 on success
*/

int mul(int a, int b)
{
	int result;

	result = a * b;
	return (result);
}
